package main // the package main declaration holds a special meaning. It indicates that the package is an executable program, not a reusable library. This is specific to programs that produce a binary executable.
import "fmt" // importing package called format
func main(){
  fmt.Println("Yash World")
  {
